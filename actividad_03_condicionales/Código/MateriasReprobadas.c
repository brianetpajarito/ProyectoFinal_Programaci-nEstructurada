//Ingresa las materias reprobadas; máximo 2 puedes reinscribirte, si no estás en situacion crítica
/*#include <stdio.h>

int main(){
    int mat;
    printf("Ingresa la cantidad de materias reprobadas: ");
    scanf("%d",&mat); // guarda el valor ingresado en mat
    if (mat<=2) { //compara si el valor de mat es menor o igual a 2
        printf("\nPuedes reinscribirte\n");
    }
    else{ //si no
        printf("\nEstas en situacion critica\n");
    }
}
*/
